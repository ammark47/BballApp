// Generated by CoffeeScript 1.9.2
(function() {
  var readFile, readFileSync, ref, ref1, updateFile, updateFileSync, writeFile, writeFileSync;

  ref = require('./read_file'), readFile = ref.readFile, readFileSync = ref.readFileSync;

  ref1 = require('./write_file'), writeFile = ref1.writeFile, writeFileSync = ref1.writeFileSync;

  updateFile = function(filePath, updater, done) {
    return readFile(filePath, function(err, object) {
      var updatedObject;
      if (err) {
        return done(err);
      }
      updatedObject = updater(object);
      return writeFile(filePath, updatedObject, done);
    });
  };

  updateFileSync = function(filePath, updater) {
    var object, updatedObject;
    object = readFileSync(filePath);
    updatedObject = updater(object);
    return writeFileSync(filePath, updatedObject);
  };

  module.exports = {
    updateFile: updateFile,
    updateFileSync: updateFileSync
  };

}).call(this);
